import React from 'react';
import ReactDOM from 'react-dom/client';
import GoinfoSalesApp from './GoinfoSalesApp';

const root = ReactDOM.createRoot(document.getElementById('root'));
root.render(
  <React.StrictMode>
    <GoinfoSalesApp />
  </React.StrictMode>
);
